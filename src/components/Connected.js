import React, { useState } from 'react';

const Connected = () => {

    return (
        <div className='connected'>
            <h1>connected</h1>
        </div>
    );
};

export default Connected;